fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9

# Media / IO
ffmpeg-python>=0.2.0
opencv-python-headless>=4.10.0
numpy>=1.26
Pillow>=10.3

# Job queue (swap for Celery/Redis in production; RQ is fine for v1)
rq>=1.16
redis>=5.0
edge-tts>=6.1.9

# ML — pin actual versions once you've chosen your models in Phase 0
# torch / torchvision — install per your CUDA version, see infra/scripts
insightface>=0.7.3
onnxruntime>=1.16.0  # Use onnxruntime-gpu for gpu profiles
# mediapipe
# gfpgan
# (LivePortrait / MuseTalk / SadTalker installed per their own setup docs
#  — these ship as repos, not clean pip packages, so infra/scripts/download_models.sh
#  vendors them into models_cache/)

pytest>=8.2
httpx>=0.27
websockets>=12.0
sqlalchemy>=2.0
psycopg2-binary>=2.9
